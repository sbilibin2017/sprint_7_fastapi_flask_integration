from unittest import mock
import pytest
from config import config
from src.databases.db import db_session


# @pytest.fixture
# def repository():
#     def inner(repo):
#         return repo
#     return inner
